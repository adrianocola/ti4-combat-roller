# (optional) Aptabase API key
VITE_PUBLIC_APTABASE=

# Path served by the Netlify Function /api/random (relative URL).
# Leave empty to use only the local Math.random() fallback.
VITE_PUBLIC_API_ENDPOINT=
